name: mobile_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.4 <3.0.0'

dependencies:
  dio: ^5.0.2
  flutter:
    sdk: flutter
  http: ^0.13.5
  provider: ^6.0.5
  carousel_slider: ^4.2.1
  intl: ^0.18.0
  mockito: ^5.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  test: ^1.22.0
  build_runner: ^2.3.3

flutter:
  uses-material-design: true

  assets:
    - assets/api/
    - assets/img/slideshow/
    - assets/img/layout/
    - assets/img/logo/